package com.example.demo.service;

import org.springframework.stereotype.Service;

@Service
public class AuthService {
/* 
    public boolean authenticateAdmin(String password) {
        // Implement logic to authenticate admin based on password only
        return "ADMIN@123".equals(password);
    }

    public boolean authenticateEmployee(String username, String password) {
        // Implement logic to authenticate employee
        return "josh".equals(username) && "honey".equals(password);
    }
*/
}
